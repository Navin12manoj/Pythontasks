package main

import "fmt"

func fact(a int)int {
   if(a <= 1) {
      return 1
   }
   return a * fact(a - 1)
}
func main() { 
   fmt.Println("Enter the number--> ")
    var a int
    fmt.Scanln(&n)
   fmt.Println(fact(a), "is the factorial of", a)
}
