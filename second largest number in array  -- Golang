package main

import "fmt"

func main() {

    var array_size int
    fmt.Println("Enter the size of array: ")
    fmt.Scan(&array_size)
    fmt.Println("Enter the array elements: ")
    num:= make([]int, array_size)
    
    for i := 0; i < array_size; i++ {

        fmt.Scan(&num[i])

    }

    value:= num[0]
    value2 := num[0]

    for i := 1; i < array_size; i++ {
        if value < num[i] {
			value2 = value
			value = array[i]
		} else if value2 < num[i] {
			valu2 = num[i]
		}

    }
